import SignupClient from '@/components/auth/SignupClient';

export const metadata = {
  title: "Create Account | smol",
  description: "Join the elite network of creators and brands using smol. Create your account to start managing your digital footprint with precision.",
};

export default function SignupPage() {
  return <SignupClient />;
}
