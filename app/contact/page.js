import ContactClient from '@/components/info/ContactClient';

export const metadata = {
  title: "Contact Support | smol",
  description: "Need assistance with your smol account or Enterprise integration? Our high-performance support team is ready to help you scale.",
};

export default function ContactPage() {
  return <ContactClient />;
}
