import ReportClient from '@/components/info/ReportClient';

export const metadata = {
  title: "Report an Issue | smol",
  description: "Help us refine the smol protocol. Report bugs, UI glitches, or security vulnerabilities directly to our intelligence team.",
};

export default function ReportPage() {
  return <ReportClient />;
}
